#include <stdio.h>
int main(){int nota=10,nota_anterior=10;if (nota >= 7)
	   if (nota_anterior < nota)
		  printf("Você está melhorando.");
	else
	   printf("Sem estudo é difícil ser aprovado.");
	
	
	
	return 0;
}